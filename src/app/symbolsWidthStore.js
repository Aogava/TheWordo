const symbolsWidth = (fontName, fontSize, symbol) => {
    let resultWidth = 0;

    switch (fontName) {
        case "Arial":
            switch (symbol) {
                case value:

                    break;

                default:
                    break;
            }
            break;

        default:
            break;
    }
    return resultWidth;
}

export default symbolsWidth;